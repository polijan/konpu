Generate preprocessor code for konpu.h to define VIDEO_WIDTH and VIDEO_HEIGHT
when KONPU_OPTION_OPTIMIZE_VIDEO_MODE is enabled.

The file modes_factor<N>.txt are generated by the `lsmode` konpu application on
the konpu printer when konpu is compiled with the VIDEO_SIZE_FACTOR set to N.
